import React from 'react';

export default React.createClass({
  render: function () {
    return (
      <div>
        Welcome to React, now with live reload and one more test!
      </div>
    );
  }
});
