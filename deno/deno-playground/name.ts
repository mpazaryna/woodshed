/**
 * Gets the user's name by prompting them.
 * @returns {string | null} The user's name or null if canceled.
 */
export function getName() {
    return prompt("What is your name?"); // Ask for the user's name
}
