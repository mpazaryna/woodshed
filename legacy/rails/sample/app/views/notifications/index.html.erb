<h1>Listing notifications</h1>

<table>
  <tr>
    <th>Path</th>
    <th>Page duration</th>
    <th>View duration</th>
    <th>Db duration</th>
    <th>Payload</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @notifications.each do |notification| %>
  <tr>
    <td><%= notification.path %></td>
    <td><%= notification.page_duration %></td>
    <td><%= notification.view_duration %></td>
    <td><%= notification.db_duration %></td>
    <td><%= notification.payload %></td>
    <td><%= link_to 'Show', notification %></td>
    <td><%= link_to 'Edit', edit_notification_path(notification) %></td>
    <td><%= link_to 'Destroy', notification, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Notification', new_notification_path %>
