# Engine Modules, Serverless, DynamoDB and API Gateway

## Run locally and save output

```sh
node ./bin/Amort.js > ./temp/amort.txt
```

### deploy to stage dev

```sh
sls deploy --stage dev
```

### deploy to stage prod

```sh
sls deploy --stage prod
```
