[pytest]
# Add the src directory to the Python path
pythonpath = src

# Optional: specify the test paths if needed
testpaths = tests

# Optional: add any other pytest options you want to configure
# e.g., to enable verbose output
addopts = -v